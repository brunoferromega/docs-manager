spring:
  application:
    name: docs-manager
  jpa:
    hibernate:
      ddl-auto: create-drop #none          # Liquibase owns the schema
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  logging:
    level:
      org.flywaydb: INFO
